# 理解邮箱提供状态操作顺序

学习者能够解释为什么 Actor 的状态读取也应通过消息完成：邮箱中的两个 `increment` 先于 `get_count` 时，消息读取确定返回 2，而绕过邮箱直接读取可能得到 0、1 或 2。后续可以进入远程 Actor、消息顺序保证和位置透明性。

## Evidence

学习者正确推导了消息读取和直接内存读取的不同结果，并指出差异来自前序消息可能尚未处理。
