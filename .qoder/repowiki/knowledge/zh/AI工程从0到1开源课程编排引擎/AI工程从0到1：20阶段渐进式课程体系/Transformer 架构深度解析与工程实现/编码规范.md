- 核心算子坚持从零实现（From Scratch），使用基础矩阵运算库（如 NumPy 或 stdlib）复现注意力机制，避免直接调用高层 API。
- 全面采用 Pre-Norm（前置归一化）残差连接模式，并优先使用 RMSNorm 替代 LayerNorm 以提升训练稳定性。
- 前馈神经网络（FFN）默认采用 SwiGLU 激活函数变体，以取代传统的 ReLU 或 GeLU，适应现代大模型架构标准。
- 文档与产出物严格实行中英双语并行维护，所有 `.md` 说明文档及 `.json` 测验文件均包含 `en` 与 `zh` 两个版本。