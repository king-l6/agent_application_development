进入 `site/vue-app/summary` 后先执行 `npm run predev` 或 `npm run prebuild` 触发快照/代码引用/Wiki 生成，再运行 `npm run dev` 本地开发；生产构建 `npm run build` 产物覆盖 `site/summary/`。站点根级 `node site/build.js` 会同步更新 `data.js`、`sitemap.xml`、`llms.txt` 及 README 中的统计数字。