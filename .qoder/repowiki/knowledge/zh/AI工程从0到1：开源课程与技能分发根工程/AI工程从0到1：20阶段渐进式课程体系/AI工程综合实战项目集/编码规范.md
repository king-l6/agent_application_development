- 每个子项目均采用“离线演示+在线服务”双模入口设计，通过命令行参数（如 `--serve`）或环境检测切换交互模式。
- 核心业务逻辑与基础设施代码严格分离，TypeScript项目中普遍采用 `server.ts` 处理路由、`orchestrator.ts` 处理状态机、`types.ts` 定义契约。
- 所有项目入口均包含自包含的冒烟测试（Smoke Test）或等价性检查（Equivalence Check），在启动时自动验证核心算法的正确性。