- 所有模块均采用单文件脚本结构，将算法实现、数据生成与结果打印逻辑封装在 `main()` 函数中，并通过 `if __name__ == "__main__":` 触发执行。
- 普遍使用 `dataclass` 定义配置对象（如 `HybridConfig`、`NSAConfig`）和结果容器，以增强代码的结构化程度和可读性。
- 在涉及性能或资源评估的模块中，统一实现自定义的格式化辅助函数（如 `fmt_bytes`、`fmt_m`），用于将原始数值转换为人类可读的单位（如 GB、Billion）。