- 所有关键工作流状态转换必须持久化到外部存储（如 JSON/PostgreSQL），以支持崩溃后的精确恢复与幂等重试。
- 高风险操作必须遵循“提议-提交”模式，包含意图、数据血缘、爆炸半径及回滚计划的结构化元数据，并强制人工确认。
- 实施多层成本控制栈，包括单次请求 Token 上限、任务级预算、时间窗口速率限制及财务速度熔断，防止“钱包拒绝服务”攻击。
- 自我改进循环必须设置边界约束，包括不变量检查、对齐锚点验证及多目标回归检测，防止能力漂移或安全退化。