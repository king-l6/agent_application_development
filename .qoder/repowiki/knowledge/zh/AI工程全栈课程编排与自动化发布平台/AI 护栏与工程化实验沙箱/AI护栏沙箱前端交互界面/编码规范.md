- 所有异步 API 调用均封装在 `api/` 目录下，统一使用自定义 `request` 函数处理 AbortController 超时逻辑与错误转换。
- 组件间通信严格遵循 Props 下行、Emits 上行的单向数据流原则，复杂状态由根组件 `App.vue` 集中管理后分发。
- 类型定义集中在 `types/index.ts`，涵盖从底层 AdapterInfo 到上层 UI 渲染块（RenderBlock）的所有数据结构，确保全链路类型安全。
- UI 样式采用 scoped CSS 结合 CSS 变量（`:root` 定义主题色），实现深色模式下的统一视觉风格与组件隔离。