- **核心管线编排**：`main.py` 实例化 `Pipeline` 并集中注册 `adapters` 模块中的各类护栏适配器，实现输入/输出阶段的短路拦截逻辑。
- **模块化实验集成**：通过 `playground.registry` 动态加载并暴露教学实验模块，支持前端按需调用特定 AI 概念演示。
- **统一 API 网关**：作为单一入口聚合聊天交互、护栏状态管理、基准测试（`benchmark`）及检查点查看等功能，并通过 CORS 中间件支撑前端交互。