- 所有模块继承 `PlaygroundModule` 并声明静态元数据（name, phase, lesson）以支持自动化分组与排序。
- 统一使用 `field_spec` 定义输入参数，通过类型标识（text, select, number）驱动前端控件渲染。
- 执行逻辑中强制捕获异常并返回包含错误信息的 `ModuleResult`，确保单个模块故障不影响整体服务稳定性。
- 采用 `block_*` 辅助函数构造结构化输出块，将业务逻辑与 UI 展示细节隔离，保持结果数据的语义化。