- 所有适配器类均定义 `name`, `display_name`, `description`, `group`, `category`, `order` 等类属性用于元数据管理。
- `check` 方法统一接收 `text` 和可选的 `context` 参数，并始终返回包含 `passed`, `reason`, `details`, `confidence` 的 `GuardrailResult` 实例。
- 在每个 `check` 方法内部使用 `time.time()` 记录执行耗时，并将结果存入 `latency_ms` 字段以支持性能监控。