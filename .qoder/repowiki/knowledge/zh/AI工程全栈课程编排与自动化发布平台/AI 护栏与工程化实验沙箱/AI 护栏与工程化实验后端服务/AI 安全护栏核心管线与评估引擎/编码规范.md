- 所有安全检测组件均继承自 `GuardrailAdapter` 基类并实现 `check` 方法，返回包含 `passed`、`confidence` 及 `latency_ms` 的标准化 `GuardrailResult` 对象。
- 管线执行采用短路逻辑，一旦某个适配器判定不通过（`passed=False`），立即终止后续检查并记录拦截详情与阻断阶段。
- 基准测试执行时会临时保存并锁定所有适配器的启用状态及内部窗口（如速率限制器），测试结束后自动恢复现场以确保不影响线上运行状态。