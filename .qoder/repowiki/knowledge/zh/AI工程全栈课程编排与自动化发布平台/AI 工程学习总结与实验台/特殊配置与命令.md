- **前置生成**：执行 `npm run predev` 或 `npm run prebuild` 触发 `gen-checkpoint-snapshot.mjs` 等脚本生成必要的数据文件。
- **本地开发**：在 `site/vue-app/summary` 目录下运行 `npm run dev`。
- **生产构建**：运行 `npm run build`，产物将覆盖 `site/summary/` 目录。