- 所有课程遵循统一的六步教学法结构：Motto -> Problem -> Concept -> Build It (从零实现) -> Use It (框架应用) -> Ship It (产出制品)。
- 课程目录采用标准化层级：`phases/<NN>-<phase-name>/<NN>-<lesson-name>/`，包含 `code/`、`docs/en.md` 和 `outputs/` 子目录。
- 产出物（Artifacts）必须包含标准化的 YAML Frontmatter（如 `name`, `description`, `tags`），以便构建脚本自动编目与分发。