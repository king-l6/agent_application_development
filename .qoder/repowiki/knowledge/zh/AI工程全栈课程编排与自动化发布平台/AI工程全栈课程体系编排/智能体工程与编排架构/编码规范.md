- 所有智能体循环实现均遵循“观察-思考-行动-停止”的四段式控制流，并强制要求设置最大迭代步数（Turn Budget）以防止死循环。
- 在涉及状态管理的模块（如 LangGraph、Workbench）中，统一采用 JSON Schema 定义状态结构，并强调状态变更的原子性与检查点（Checkpoint）持久化。
- 工具调用（Tool Use）必须包含严格的参数校验与错误观察反馈机制，确保模型能根据工具返回的非预期结果（如 400 错误）进行自我修正。
- 多智能体协作场景下，优先采用基于消息传递（Message Passing）或共享状态图的解耦设计，以实现故障隔离与并发执行。