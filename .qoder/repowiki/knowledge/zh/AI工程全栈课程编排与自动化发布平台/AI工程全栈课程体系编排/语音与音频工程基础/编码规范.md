- 所有音频处理脚本默认采用16kHz单声道浮点数组作为标准输入格式，以确保与Whisper等主流模型的兼容性。
- 在涉及采样率转换或降采样时，强制要求先进行低通滤波以防止混叠，并在代码中通过对比实验（如`main.py`中的aliasing演示）验证该原则。
- 实时音频流处理统一采用20ms分帧（chunking）机制，并在VAD gating后缓冲至少300ms语音以消除首词截断问题。
- 在评估ASR性能时，统一使用字错误率（WER）作为核心指标，并在计算前执行标准化处理（小写化、去除标点）。