- 在 `code/main.py` 中优先使用 Python 标准库（re, math, collections）从零实现核心算法，以揭示底层逻辑，随后在文档中引入第三方库作为生产对照。
- 每个子模块均遵循统一的目录模板，包含 `docs/`（理论）、`code/`（实践）、`assets/`（可视化）和 `quiz.json`（考核），确保学习路径的一致性。
- 在涉及矩阵运算或向量处理的代码中（如注意力机制、Embedding），通过显式的形状注释（Shape Comments）和维度检查来预防实现错误。
- 为每个关键技术点提供“Ship It” Prompt 模板，将算法选择标准（如分词器选型、分块策略）封装为可复用的工程决策逻辑。