- **线性阶段拓扑**：`phases/00~19` 按数字顺序构成单向依赖链（Setup → Math → ML → DL Core → CV/NLP/Speech → Transformers → Generative AI → RL → LLM From Scratch → LLM Engineering → Multimodal → Tools & Protocols → Agent Engineering → Autonomous Systems → Multi-Agent → Infrastructure → Ethics → Capstone），每个阶段 README 统一引用根级 `ROADMAP.md` 中的全局依赖图。
- **标准化单元契约**：所有课程子目录强制采用 `code/` + `docs/` + `outputs/` + `quiz.json` + `quiz.zh.json` 五件套结构，配合六步教学法（Motto → Problem → Concept → Build It → Use It → Ship It）保证跨阶段知识迁移一致性。
- **双轨实现范式**：核心算法在每个阶段同时提供“从零实现”（纯Python/NumPy/Math）与“框架应用”（PyTorch/Sklearn/JAX等）两套代码，打通底层原理与工程应用的认知隔阂。
- **Artifacts 驱动输出**：每个单元必须产出可复用的工程制品（Prompt模板、Agent Skills、MCP服务器或独立Agent），并通过统一脚本注入主流AI编程助手环境，使学习过程直接沉淀为生产资产。