- **渐进式分层结构**：模块按“数学原理(01-05) -> 训练策略(06-09) -> 框架集成(10-13)”的逻辑递进，每个子目录包含文档、代码实现与技能输出。
- **双轨制技术验证**：在 `10-mini-framework` 中通过纯 Python 实现 `Module`、`Linear`、`Sequential` 等核心类以解构自动微分；随后在 `11-intro-to-pytorch` 和 `12-intro-to-jax` 中对比工业级框架的张量运算与计算图机制。
- **标准化产出闭环**：每个知识点均配备双语文档 (`docs/`)、 quiz 测验以及针对特定工程场景的 Prompt 输出 (`outputs/`)，形成“理论-代码-应用”的完整学习链路。