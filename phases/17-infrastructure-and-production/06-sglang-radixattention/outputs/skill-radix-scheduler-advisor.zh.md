---
name: radix-scheduler-advisor
description: 为希望利用 RadixAttention 缓存重用的前缀繁重工作负载提供 SGLang 采用和提示排序纪律的建议。
version: 1.0.0
phase: 17
lesson: 06
tags: [sglang, radixattention, prefix-caching, scheduler, prompt-ordering]
---

给定工作负载描述（提示模板形状、检索模式、对话长度、并发租户数、硬件），产生一个 SGLang / RadixAttention 采用建议。

产出：

1. **工作负载指纹。** 分类为前缀繁重（具有重复前导的 RAG、具有重复工具模式的 Agent、具有重复上下文的语音）或前缀轻量（唯一的单次提示）。指出共享前缀长度和重复率。
2. **提示排序审计。** 从上到下遍历当前提示模板。标记任何插入到不可变部分中的动态内容。推荐规范顺序：系统 -> 工具/模式 -> 检索上下文 -> 对话历史 -> 用户输入。
3. **预期命中率。** 从工作负载指纹估算可实现的缓存命中率。通用对话 10-30%。具有一致模板的 RAG 60-85%。具有固定前导的语音/视觉 80-95%。
4. **SGLang vs vLLM 决策。** 如果预期命中率 > 40% 且工作负载不是单次生成，推荐 SGLang。如果 < 30%，使用 `--enable-prefix-caching` 的 vLLM 更简单。如果 30-40%，在样本上运行两者并选择。
5. **推出计划。** 在当前提示模板上对 SGLang 进行 48 小时影子基准测试。记录命中率。修复提示排序问题。重新基准测试。如果命中率超过目标则投产。

硬性拒绝：
- 不测量流量中实际的前缀共享就推荐 SGLang。拒绝。
- 没有引用工作负载形状就声称 6.4 倍数字。该数字是工作负载特定的。
- 忽略提示排序纪律。模板就是缓存键；没有它，调度器无法提供帮助。

拒绝规则：
- 如果工作负载是单次生成（无重复系统提示），拒绝 SGLang 并推荐 vLLM。
- 如果团队无法控制提示模板（第三方消费者），拒绝并在重新评估前推荐代理层模板规范化。
- 如果多租户隔离需要每租户单独的 KV 池，请注意 SGLang 支持此功能，但树分支驱逐可能使较小租户匮乏；推荐每租户预算分配。

输出：一页 SGLang 建议，列出工作负载指纹、提示排序修复、预期命中率、引擎选择和推出计划。最后给出一个"下一步阅读什么"段落，根据最大差距指向 SGLang 论文、vLLM 前缀缓存文档或本课中的提示排序练习。
