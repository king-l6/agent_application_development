---
name: prompt-tokenizer-builder
description: 为LLM项目构建和调试生产质量的分词器
version: 1.0.0
phase: 10
lesson: 2
tags: [tokenizer, bpe, byte-level, special-tokens, chat-template, multilingual]
---

# 生产分词器构建器

在为LLM项目构建或调试分词器时，请遵循此框架。

## 管道检查清单

每个生产分词器需要这五个阶段。如果缺少其中一个，你会在生产环境中遇到边缘情况。

1. **归一化**——应用NFKC Unicode归一化。这折叠连字（"fi" -> "fi"），归一化全角字符，并标准化空白。跳过这一步，同一个词会根据输入方式得到不同的token ID。

2. **预分词**——在BPE之前将文本分割成块。对以英语为中心的模型使用GPT-2的正则表达式模式。对多语言模型使用SentencePiece的原始字节方法。选择决定了BPE是否可以跨词边界合并。

3. **BPE合并**——在每个块内将学到的合并表应用于字节序列。合并表就是分词器学到的知识。其他都是管道工程。

4. **特殊Token注入**——在BPE运行之前精确匹配特殊token。[BOS]、[EOS]、[PAD]、聊天模板标记获得固定ID。它们从不参与合并。

5. **ID映射**——将token字符串转换为整数。模型只看到整数。

## 调试分词器问题

**症状：模型在聊天输入上产生垃圾**
- 检查聊天模板。每个模型有不同的格式。Llama 3使用`<|start_header_id|>`标记。ChatGPT使用`<|im_start|>`标记。错误的模板将输入置于训练分布之外。

**症状：非英语文本使用太多token**
- 检查生育率（每词token数）。高于2.0意味着分词器在该语言上浪费上下文窗口。解决方案：用更多多语言数据重新训练、增加词汇表大小、或使用SentencePiece与Unigram。

**症状：数字和算术失败**
- 检查数字如何被分词。"1234"作为一个token意味着模型不能进行数字级操作。在预分词期间单独分割数字。

**症状：代码token效率低下**
- 检查缩进如何处理。GPT-2的分词器在空格上浪费token。Codex和StarCoder使用特殊的缩进token（4个空格 = 1个token）。

## 词汇表大小决策

- 32K token：单语言、小模型、有限计算。嵌入层为32K * d_model参数。
- 50K-64K：多语言或代码密集型。对大多数项目是好的平衡。
- 100K+（GPT-4、Llama 3）：仅当有大量训练数据时。序列更短但100K * d_model嵌入参数。

对于4096维模型：32K词汇表 = 1.31亿嵌入参数。128K词汇表 = 5.24亿嵌入参数。仅嵌入层就有4亿参数的差异。

## 速度要求

- 训练数据分词：使用Rust支持的库（tiktoken、HuggingFace tokenizers）。纯Python慢10-100倍。
- 推理分词：延迟不那么重要（单序列），但仍使用编译实现。
- 基准：分词1GB文本并测量挂钟时间。如果超过60秒，切换到Rust后端。

## 聊天模板验证

在部署任何聊天模型前，验证模板：

1. 用分词器编码一个已知对话
2. 解码回文本
3. 逐字符与模型文档中的预期格式比较
4. 注意：头token后的换行、内容前的空格、轮次结束标记
5. 测试边缘情况：空系统消息、超长用户消息、多个助手轮次

搞错聊天模板是聊天模型性能退化的最常见来源。
